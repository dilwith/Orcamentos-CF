{
  "compilerOptions": {
    /* Linguagem e ambiente */
    "target": "ES6",                     // Ou "ES2022", se quiser algo mais recente
    "module": "Commonjs",                   // Mantém CommonJS (compatível com Node 14+)
    "lib": ["ES2020"],                      // Inclui as definições ES2020
    
    /* Organização de pastas */
    "rootDir": "./src",                       // Pasta principal do código TS
    "outDir": "./dist",                       // Onde serão gerados os arquivos compilados

    /* Resolução de módulos */
    "moduleResolution": "node",             // Padrão de resolução de módulos Node
    "resolveJsonModule": true,              // Permite importar arquivos .json

    /* Emissão de código */
    "esModuleInterop": true,                // Facilita import de módulos CommonJS
    "sourceMap": true,                      // Gera arquivos .map para debug
    // "removeComments": true,              // Descomente se quiser remover comentários no build

    /* Verificações */
    "strict": true,                         // Ativa todas as checagens estritas
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true                    // Ignora checagem de arquivos .d.ts de libs
  },
  "include": ["src"],                       // Quais pastas/arquivos o TS deve compilar
  "exclude": ["node_modules"]               // Quais pastas/arquivos o TS deve ignorar
}
